/**
 * Corner radii. Named for role, not size, so the design identity
 * session can retune the scale without touching call sites.
 */
export const radius = {
  card: 18,
  frame: 24,
  pill: 999,
};
