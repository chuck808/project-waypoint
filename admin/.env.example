PUBLIC_SUPABASE_URL=
PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Base URL printed inside QR posters (ADR-004: the production value is
# permanent once signs are printed -- confirm domain registration first).
# For local device testing, a LAN address of apps/web works.
