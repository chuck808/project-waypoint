# Steward Content Helpers

`src/lib/waypoint-content.ts` exposes shared Waypoint copy, examples and prompt text from `@waypoint/ui` for the Steward Portal.

If this app does not yet list `@waypoint/ui` in `business/package.json`, add it as a workspace dependency:

```json
"@waypoint/ui": "workspace:*"
```
